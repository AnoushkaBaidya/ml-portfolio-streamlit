"""
Central application configuration.

This file stores constants that are shared across the application.
As the portfolio grows, this becomes the single place to define
paths, app metadata, default random states, and global settings.
"""

APP_NAME = "ML Portfolio Streamlit"
APP_VERSION = "0.1.0"
DEFAULT_RANDOM_STATE = 42